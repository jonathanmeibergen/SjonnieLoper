@page
@model SjonnieLoper.Pages.Products.CreateModel
@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<form method="post">
    <select asp-for="productAddedID"
            asp-items="@(new SelectList(Model.RegisteredWhiskeyTypes, "Value", "Text"))">
    </select>
    <input type="hidden" asp-for="@Model.Whiskey.WhiskeyId">
    <label asp-for="Whiskey.Name"></label>
    <select asp-for="productAddedID"
            asp-items="@(new SelectList(Model.RegisteredWhiskeyTypes, "Value", "Text"))">
    </select>


    <button type="submit" class="btn btn-outline-primary">Confirm</button>
</form>